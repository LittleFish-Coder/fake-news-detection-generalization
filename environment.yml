name: fake-news
channels:
  - defaults
dependencies:
  - python=3.10
  - pandas
  - transformers
  - numpy
  - pytorch
  - scikit-learn
  - pip
  - pip:
    - evaluate
    - accelerate
    - ipykernel
    - gensim